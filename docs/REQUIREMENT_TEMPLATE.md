# AI 业务开发需求模板

给 AI 开发 Code 项目业务代码时，建议按以下格式描述需求。

完整业务契约以 `docs/任务设计模板.md` 为唯一母版。本文只用于用户提交需求时快速补齐关键信息，AI 在实现前应按 `docs/任务设计模板.md` 输出可审查契约。

## 背景

- 项目名称：
- 影刀已经完成的动作：
- Python 需要完成的动作：
- 是否允许修改影刀流程：默认否

## 任务摘要

- 任务名称：
- `tasks[].type`：
- handler 职责：
- 业务处理步骤：
- 人工确认点：

## Payload 字段

| 字段 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
|  |  |  |  |  |

## 输入线索

影刀应准备的 `input.json` 由 AI 按 `docs/任务设计模板.md` 拟定并提交确认。需求描述中可先提供以下线索：

- `data/input/` 中会有哪些文件：
- 文件格式、字段名、编码、分隔符：
- 影刀是否还需要读取业务输出文件：
- 已知的 `payload` 字段含义：

## 输出

- 业务输出文件路径：
- 输出格式：
- 关键字段：
- 空数据、重复数据、异常数据处理规则：

## 状态与异常

- 何时返回 `success`：
- 何时返回 `warning`：
- 何时返回 `retryable_error`：
- 何时返回 `pending_fix` 或 `fatal`：

## 验收

- 本地验证命令：
- 预期输出文件：
- 预期 `runner_{run_id}.json.status`：
- 需要补充的测试：
